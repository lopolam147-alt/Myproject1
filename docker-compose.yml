version: '3.8'
services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: lpl
      POSTGRES_PASSWORD: lpl01470
      POSTGRES_DB: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresqldocker-compose down
volumes:
  postgres_data: